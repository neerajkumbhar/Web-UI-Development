A small POC of a registeraion page where the data entered by the user 

is being stored in the session storage.

Sample code used for session storage:

sessionStorage.setItem("User ID",0);
var user = document.getElementById("userid");
sessionStorage.setItem("User ID", user.value);  
